const buttons = document.querySelectorAll('.book-now-btn');
buttons.forEach(button => {
    button.addEventListener('click', () => {
        alert('Redirecting to the booking page!');
        // Add your redirect or booking functionality here
    });
});
/**
 * 
 */